# Should-cost methodology

Per-unit cost combines net material divided by yield, machine and labor time, allocated setup/tooling, direct additions, overhead, margin and risk allowance. Currency uses decimal arithmetic. Every calculation must persist units, currency, input assumptions, model version, validation and approval status. Sensitivity analysis changes one named assumption at a time while preserving the baseline.
