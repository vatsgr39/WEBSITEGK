# AI safety

Calculation engines and authorization remain deterministic. Provider keys stay server-side. Retrieved text is data, never instruction; suspicious instruction-like uploaded content is quarantined. Responses include internal record citations, confidence and limitations. The Copilot cannot mutate records without an explicit confirmed workflow and uses templates when no provider key is configured.
