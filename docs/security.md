# Security design

Target controls include Argon2 passwords, short access tokens, hashed rotating refresh tokens, least-privilege role checks, supplier row filtering, CSP/security headers, throttling and lockout, strict CORS, upload allowlists, formula-injection escaping, immutable audit events, non-root containers and secret-manager injection. See the root security policy for current limitations.
