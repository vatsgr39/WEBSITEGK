# Scenario methodology

Scenarios are immutable event sets evaluated against a named baseline. Expected disruption cost is probability × downtime days × daily business impact × dependency. Monte Carlo outputs P10/P50/P90, iteration count and a reproducible seed; simulated values must be labeled separately from deterministic totals.
